plugins {
    id 'com.google.cloud.tools.jib' version '3.3.1'
}

subprojects {
    apply plugin: 'com.google.cloud.tools.jib'

    jib {
        from {
            image = "$baseImage"
        }
    }
}